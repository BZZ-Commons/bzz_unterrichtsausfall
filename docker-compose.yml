name: schulausfall

services:
  app:
    build: .
    image: schulausfall:latest
    container_name: schulausfall
    ports:
      - "127.0.0.1:3002:3000"
    restart: always
    env_file:
      - .env
