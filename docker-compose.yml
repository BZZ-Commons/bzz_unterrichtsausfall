name: unterrichtsausfaelle

services:
  app:
    build: .
    image: unterrichtsausfaelle:latest
    container_name: unterrichtsausfaelle
    ports:
      - "127.0.0.1:3002:3000"
    restart: always
    env_file:
      - .env
